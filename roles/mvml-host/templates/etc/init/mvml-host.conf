description "MVML hosting application"
author      "Justin Scott"

start on (local-filesystems and net-device-up)
stop on shutdown

respawn

script
  #echo $$ > /var/run/mvml-host.pid
  exec /usr/bin/nodejs /opt/mvml/host/server.js 80 >> /var/log/mvml-host.log 2>&1
end script

post-start script
  exec echo '--restarted--' >> /var/log/mvml-host.log
end script
